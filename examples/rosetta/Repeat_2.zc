fn times(proc: fn(), n: uint) {
    for _ in 0..n {
        proc();
    }
}

// Reusing the 'times' procedure defined above
fn main() {
    let count = 0;
    times(fn[&]() {
        count++;
        println "Iteration {count}";
    }, 5);
}
